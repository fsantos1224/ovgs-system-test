services:
  api:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
      - ovgs-data:/app/data
    command: node server.cjs
    ports:
      - "3001:3001"

volumes:
  ovgs-data:

  frontend:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - api
